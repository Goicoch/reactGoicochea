import { useEffect, useState } from 'react';

const Greeting= ({name}) => {

    return (
        <div>
            <h1> Hola capo {name}</h1>
        </div>
         );
        }

export default Greeting